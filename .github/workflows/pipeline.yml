# ================================================================
# Salesforce Delta Deployment Pipeline
#
# FLOW:
# 1. Developer creates PR  → Validation runs automatically
# 2. Validation passes     → Reviewer approves PR manually  
# 3. PR merged             → Actual deployment runs
#
# BRANCH → ORG MAPPING:
# DEV   → DEV Sandbox     (NoTestRun)
# SIT   → SIT Sandbox     (RunLocalTests)
# UAT   → UAT Sandbox     (RunLocalTests)
# main  → PRODUCTION      (RunAllTestsInOrg)
# ================================================================

name: Salesforce Delta Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - DEV
      - SIT
      - UAT
      - main
  pull_request:
    branches:
      - DEV
      - SIT
      - UAT
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AUTH_URL: ''
      ENV_NAME: ''
      ORG_ALIAS: ''
      TEST_LEVEL: ''

    steps:

      - name: Checkout Full Git History
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Salesforce CLI
        run: |
          npm install --global @salesforce/cli
          sf --version

      - name: Install sfdx-git-delta
        run: echo y | sf plugins install sfdx-git-delta

      - name: Set Environment Variables by Branch
        run: |
          if [ "${{ github.ref_name }}" == "DEV" ] || \
             [ "${{ github.base_ref }}" == "DEV" ]; then
            echo "AUTH_URL=${{ secrets.DEV_ORG }}" >> $GITHUB_ENV
            echo "ORG_ALIAS=dev" >> $GITHUB_ENV
            echo "TEST_LEVEL=NoTestRun" >> $GITHUB_ENV
            echo "ENV_NAME=DEV" >> $GITHUB_ENV

          elif [ "${{ github.ref_name }}" == "SIT" ] || \
               [ "${{ github.base_ref }}" == "SIT" ]; then
            echo "AUTH_URL=${{ secrets.SIT_ORG }}" >> $GITHUB_ENV
            echo "ORG_ALIAS=sit" >> $GITHUB_ENV
            echo "TEST_LEVEL=RunLocalTests" >> $GITHUB_ENV
            echo "ENV_NAME=SIT" >> $GITHUB_ENV

          elif [ "${{ github.ref_name }}" == "UAT" ] || \
               [ "${{ github.base_ref }}" == "UAT" ]; then
            echo "AUTH_URL=${{ secrets.UAT_ORG }}" >> $GITHUB_ENV
            echo "ORG_ALIAS=UAT" >> $GITHUB_ENV
            echo "TEST_LEVEL=RunLocalTests" >> $GITHUB_ENV
            echo "ENV_NAME=UAT" >> $GITHUB_ENV

          elif [ "${{ github.ref_name }}" == "main" ] || \
               [ "${{ github.base_ref }}" == "main" ]; then
            echo "AUTH_URL=${{ secrets.PRODUCTION_ORG }}" >> $GITHUB_ENV
            echo "ORG_ALIAS=production" >> $GITHUB_ENV
            echo "TEST_LEVEL=RunAllTestsInOrg" >> $GITHUB_ENV
            echo "ENV_NAME=PROD" >> $GITHUB_ENV
          fi

      - name: Authenticate to Salesforce (${{ env.ENV_NAME }})
        run: |
          echo "$AUTH_URL" | sf org login sfdx-url \
            --alias "$ORG_ALIAS" \
            --set-default \
            --sfdx-url-stdin

      - name: Generate Delta Package
        run: |
          mkdir -p delta
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            FROM_REF="origin/${{ github.base_ref }}"
          else
            FROM_REF="HEAD~1"
          fi

          sf sgd source delta \
            --to HEAD \
            --from "$FROM_REF" \
            --output-dir delta \
            --source-dir force-app \
            --generate-delta
          echo "====== Delta Components for $ENV_NAME ($ORG_ALIAS) ======"
          cat delta/package/package.xml

      - name: Validate Delta (${{ env.ENV_NAME }})
        if: github.event_name == 'pull_request'
        run: |
          if grep -q '<members>' delta/package/package.xml; then
            VALIDATE_TEST_LEVEL="$TEST_LEVEL"
            if [ "$VALIDATE_TEST_LEVEL" == "NoTestRun" ]; then
              VALIDATE_TEST_LEVEL="RunLocalTests"
            fi
            sf project deploy validate \
              --manifest delta/package/package.xml \
              --target-org "$ORG_ALIAS" \
              --test-level "$VALIDATE_TEST_LEVEL" \
              --wait 60
          else
            echo "No Salesforce metadata changes found. Skipping validation."
          fi

      - name: Deploy Delta to ${{ env.ENV_NAME }}
        if: github.event_name == 'push'
        run: |
          if grep -q '<members>' delta/package/package.xml; then
            sf project deploy start \
              --manifest delta/package/package.xml \
              --target-org "$ORG_ALIAS" \
              --test-level "$TEST_LEVEL" \
              --wait 60
          else
            echo "No Salesforce metadata changes found. Skipping deployment."
          fi

      - name: Deploy Destructive Changes
        if: github.event_name == 'push'
        run: |
          if [ -f "delta/destructiveChanges/destructiveChanges.xml" ] && \
             grep -q '<types>' delta/destructiveChanges/destructiveChanges.xml; then
            sf project deploy start \
              --manifest delta/destructiveChanges/package.xml \
              --post-destructive-changes delta/destructiveChanges/destructiveChanges.xml \
              --target-org "$ORG_ALIAS" \
              --test-level NoTestRun \
              --wait 30
          else
            echo "No destructive changes found. Skipping."
          fi